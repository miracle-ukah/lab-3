#include "Header.h"
#include <iostream>

void print(double Begin, double End, double Interval, double x, func s)
{
    int j = 0;
    double* a = new double[double((End - Begin) / Interval) + 1];
    for (int i = Begin; i <= End; i += Interval)
    {
        a[j] = s(i, x);
        J++;
    }
    std::cout << std::endl;
    for (int i = 0; i < j; i++)
    {
        std::cout << i << " equal " << a[i] << std::endl;
    }
    delete[] a;
}
