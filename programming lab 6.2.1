#include <iostream>
#include <cmath>
#include "Header.h"
using namespace std;

double function1(int x, int n)
{
    double s, y = 0;
    if (x <= 0)
    {
        for (int i = 2; i <= (n - 1); i++)
        {
            y += (x - 1) / i;
        }
    }
    else
    {
        for (int i = 0; i <= (n - 1); i++)
        {
            s = 0;
            for (int j = 0; j <= n; j++)
            {
                s = s + ((i + 1) / (j + x));
            }
            y *= s;
        }
    }
    return y;
}

double function2(int x, int n)
{
    double root = ((pow(x, 4)) - (pow(x, 2)) - x + n);
    return root;
}

int main()
{
    int Begin, Stop, Step, x, n;
    cout << "Begin: ";
    cin >> Begin;
    cout << "Stop: ";
    cin >> Stop;
    cout << "Step: ";
    cin >> Step;
    cout << "n (n > 3): ";
    cin >> n;

    cout << "the greatest root is (function 1): " << greatestRoot(function1, Begin, Stop, Step, n) << endl;
    cout << "the greatest root is (function 2): " << greatestRoot(function2, Begin, Stop, Step, n) << endl;

    return 0;
}
